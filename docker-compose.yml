services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - /root/dasha_data:/app/data
      - /root/dasha_images:/app/images
      - dasha_logs:/app/logs

volumes:
  dasha_logs:
